<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:name=".PcBuilderApp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PCBuddyBuilder"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".CategoryProductsActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".AddProductsActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".BuildCustomPcActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ActivityIndividualChat" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".ProductsSearchActivity" />
        <activity android:name=".FORGOTPasswordActivity" />




    </application>

</manifest>